{
  "name": "classroom",
  "version": "1.0.0",
  "description": "Stryke Force classroom materials",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "browser-sync start -cwd build --server --directory --files */index.html",
    "build": "gulp buildAll"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "gulp": "^4.0.2"
  },
  "dependencies": {
    "execa": "^5.0.0",
    "reveal.js": "^4.1.0"
  }
}
